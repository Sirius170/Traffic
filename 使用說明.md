# OpenTrafficLab 檢查與修改

此資料夾是原專案副本；Downloads 原始檔案未修改。

## 執行
在 MATLAB 開啟本資料夾的 START_HERE.m，按 Run。
這是固定時制十字路口車輛模擬，尚未加入最佳化。
可調整 inflow（四方向車流量）、phaseSeconds（四相位秒數）、simulationSeconds 和 playbackSpeed。
紅綠線表示允許或禁止通行的路徑，不是實體號誌燈模型。

## 已修改
- 四路口單一 InjectionRate 展開由三方向修正為四方向。
- TrafficController 使用空圖形 handle 陣列，避免以一條空線初始化後跳過路徑線建立。
- 線條改為標準 RGB 顏色，hold 明確指定座標軸。
- 新增 START_HERE.m，設定路徑、俯視畫面與播放速度。

## 驗證限制
已檢查原始碼；MATLAB 批次啟動在載入專案前報 File system inconsistency，未完成動態測試。
原專案記載測試於 R2020b；R2025b 尚未驗證。
原專案使用 driving.scenario 內部介面，可能仍需版本相容性修改。
如有錯誤，請提供 MATLAB 完整錯誤文字與行號。
原 TrafficController.restart 仍含未完成的原始範本；本入口每次重建場景，不使用 restart。

## R2025b ReferencePoint 修正
根據使用者實測的 actorPoses 錯誤，DrivingStrategy 已新增 ReferencePoint = rear-axle，對應原程式直接更新車輛 Position 的方式。已對照本機 R2025b actorPoses 與 Vehicle 原始碼；完整模擬仍待重跑確認。

## R2025b 感測器場景初始化調整
使用者第二次執行已通過 ReferencePoint，但在 SensorSim/addActors 報無效資料。新增 initializeOTLScenario：暫時將演員設為可見，以有限姿態初始化 SensorSim，再透過 onCleanup 恢復原始可見性；不改進場時間、位置或推進時間。START_HERE 已呼叫此函式。此修正根據本機 actorPoses 對隱藏物件輸出 NaN 的原始碼推導，尚待使用者重跑驗證；不可視為已完成相容性測試。
